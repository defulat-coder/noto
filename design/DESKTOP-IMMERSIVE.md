# PC 端沉浸式液态玻璃

2026-09-13。本次只修改 macOS 界面。

- 主窗口使用连续的桌面采样材质与原生 Liquid Glass，导航、阅读和对话共享同一背景。
- 去掉侧栏围框、对话竖线、日历分区线、设置分隔线和任务卡片描边。
- 看板不再绘制整列背景，用标题、留白和轻微明暗组织任务。
- 选中任务以轻微色调标记；系统键盘焦点和原生菜单的操作分组保留。
- 设置、任务编辑与最近删除弹窗使用同一玻璃表面。
- 浮在历史记录上方的新建记录编辑器保持实底，避免历史文字透入。
- 遵循系统深浅外观与降低透明度设置；旧系统使用原生材质回退。

实现：窗口后方由 NSVisualEffectView 的 behindWindow 材质采样；macOS 26 叠加原生 clear Glass 表面。整个窗口只使用一个主要玻璃背景，避免把每个内容区都围成玻璃卡片。

验证：58 项 Swift 测试，0 失败、1 项外部条件测试跳过。实际原生预览检查了浅色主窗口、深色无分隔线日历和玻璃任务编辑弹窗；在内存预览中完成输入、保存、返回日历与查看新任务。截图见本次任务的 Computer Use 输出。最终构建和签名检查通过。

本次没有修改数据层，没有重新进行真实 AI / 云同步联调。
