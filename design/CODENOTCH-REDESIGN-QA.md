# Codenotch 参考重做 · 2026-09-13

final result: passed

范围：现有 Noto macOS 前端；保留 SQLite、CLI、同步与 AI 的业务入口。

## 视觉依据与证据

- 源仓库：https://github.com/vinzdg/codenotch ，参考提交 `bd62f44`。
- 已安装参考：`/Applications/Codenotch.app`。通过原生 UI 工具捕获账号及外观设置，680 × 520 展示像素；实际选项为小尺寸、左侧、悬停展开、液态玻璃。
- 源视觉稿：仓库 `docs/design/frame-124-hover-tooltip.png`，原图 2000 × 2000，工具展示缩放为 1600 × 1600。它是纯黑药丸的构图依据，不与通透设置页进行逐像素比较。
- 实现：`/tmp/Noto-Design.app` 独立 bundle 的内存预览；620 × 700 紧凑主窗口、1340 × 954 宽主窗口、680 × 520 设置页。
- 截图证据在本任务的原生 UI 工具输出中，工具未返回截图文件路径。设置页的源截图与实现截图在同一个工具输出中并列提交比较，两者展示尺寸一致，无额外密度归一化；原生点尺寸用于布局，非浏览器 CSS。
- 另检查记录、看板、日历、收起侧栏、深色对话、药丸左/右/上/下与通透/纯黑状态。字体、分组、图标与控件在 680 × 520 / 620 × 700 截图中均可直接辨认，无需再裁切放大。

## 对照与修正

| Before | After | Why |
| --- | --- | --- |
| 侧栏与主内容同一平面 | 4pt 内缩、圆角玻璃侧栏，独立内容背景 | 对齐参考的浮动侧栏层级 |
| 设置是一整列长表单 | 账号与同步 / AI / 外观 / 通用 | 与参考按主题组织控件的方式一致 |
| 左对齐的切换控件 | 标签左侧、开关与分段选择右侧 | 对齐设置页的行节奏 |
| 药丸内部容器隐式居中，图标偏离箭头 | 填满布局容器并从顶部定位 | 修复预览中实际观察到的 P1 错位；后续截图环与箭头已对齐 |
| 药丸选中背景出现方形色块 | 移除整行方形背景，保留圆形按钮反馈 | 后续原生预览未再出现越过轮廓的色块 |
| 无内容时卡片过高 | 内容相关高度、提示卡沿目标元素移动 | 减少空白并保持箭头位置 |
| 白底 / 深色混用硬编码黑字 | 语义前景色与纯黑模式的深色环境 | 深色窗口、卡片与按钮可辨认 |

字体采用与参考同类的 SF 系统字体，侧栏 13pt、图标 20pt、设置标题 16pt；保留 Noto 正文 15pt，适合长记录阅读。圆角组块、低对比度分隔和原生分段控件统一。图标使用 SF Symbols，未引入栅格仿制品牌图。贴边轮廓改编自参考的真实 Shape 源码，MIT 许可保留并打包进应用。文案映射为记录、待办、AI 与同步，不复制参考的额度服务功能。

## 已执行验证

- 原生 UI：记录 / 看板 / 日历切换；展开 / 收起导航；⌘N 打开录入；粘贴并保存新记录；⌘K 搜索可找到保存的记录；创建任务后在看板出现；记录菜单进入 AI 对话。
- 窄窗口：620pt 记录、日历、任务编辑，导航与搜索可达；看板保留横向滚动，不压缩卡片到不可阅读。
- 深色：主窗口、侧栏、对话及药丸；通透与纯黑药丸四向提示卡。药丸使用生产组件的独立预览，而非截图替代组件。
- `swift test`：54 tests，0 failures，1 skipped。包括新增的无日期任务排除、跨午夜到期统计、四向轮廓边界、图标布局不重叠检查。
- release 构建和 ad-hoc codesign 验证通过；见 `design/codenotch-redesign/build.log`。
- `git diff --check` 通过。

## 验证边界

未重新调用真实 AI CLI、登录同步账号或验证外部服务。外部服务测试按现有条件跳过。屏幕贴边窗口的连续指针轨迹、⌥ 拖动、多显示器 / 全屏切换没有通过原生 UI 工具进行完整自动化；已修复相应计时、方向和几何代码，四向组件视觉及数据逻辑有检查。本次没有发布或覆盖 Applications 中的安装版本。

本报告通过的是已列明的 macOS 视觉与交互范围，不代表上述系统场景已全部实机验收。
